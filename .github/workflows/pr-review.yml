name: night-pr-reviewer

on:
  schedule:
    # Every 15 min — PR review scan
    - cron: '*/15 * * * *'
    # Daily digest trigger (7am UTC = 12:30pm IST)
    - cron: '0 7 * * *'
  # Allow manual trigger from Actions tab for testing / video demo
  workflow_dispatch:

# Prevent overlapping runs if a scan takes longer than the interval
concurrency:
  group: pr-reviewer
  cancel-in-progress: false

jobs:
  review:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install deps
        run: pip install -r requirements.txt

      # Cache the logs/ dir across runs so the daily digest can see them
      - name: Restore logs cache
        uses: actions/cache@v4
        with:
          path: logs
          key: pr-reviewer-logs-${{ github.run_id }}
          restore-keys: |
            pr-reviewer-logs-

      - name: Run PR reviewer
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN_PAT: ${{ secrets.PR_REVIEWER_PAT }}
          REPOS: ${{ secrets.REPOS }}
          # Opt-in to auto-close. Defaults to false if unset.
          # The agent will close a PR only if ALL three pass:
          #   verdict=request_changes AND confidence=high AND severity_score>=9
          ALLOW_AUTO_CLOSE: ${{ vars.ALLOW_AUTO_CLOSE || 'false' }}
        run: python pr_reviewer.py

      - name: Save logs cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: logs
          key: pr-reviewer-logs-${{ github.run_id }}

      # Daily digest — only runs on the 7am UTC schedule trigger
      # 7am UTC = 12:30pm IST. Change to '0 2 * * *' for 7:30am IST if you prefer.
      - name: Send daily digest
        if: github.event.schedule == '0 7 * * *'
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          DIGEST_RECIPIENT: ${{ secrets.DIGEST_RECIPIENT }}
        run: python send_digest.py
