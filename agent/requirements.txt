anthropic>=0.40.0
requests>=2.31.0
supabase>=2.0.0
langgraph>=0.2.0
# Used by pr_reviewer.py to sign GitHub App JWTs (multi-tenant
# installation-token minting). Often pulled in transitively by other
# deps, but pinned explicitly so a fresh install never lands on a
# version without the public-key primitives we need.
cryptography>=42.0
# Used by terminal_server.py (the "house terminal") to serve an
# interactive PTY over a WebSocket to the dashboard /dashboard/terminal
# tab. Only needed on the box that hosts the shared runner.
websockets>=12.0
